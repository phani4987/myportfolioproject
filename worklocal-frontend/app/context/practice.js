const db = require('./db');
exports.signup = (req,res)=>
{
    const {firstname,lastname}= req.body;
    const sql = 
    
    ``;
    db.query(sql,[],(err,result)=>
    {
        if(err)
        {
            
        }
    })
}